{
  "name": "metacognitive-orchestrator",
  "private": true,
  "workspaces": [
    "brain-core",
    "agents",
    "ui",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev": "concurrently -n server,ui -c blue,green \"npm run dev --workspace=server\" \"npm run dev --workspace=ui\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
